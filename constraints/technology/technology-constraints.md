# Technology Constraints

**Owner:** Tech Lead / Platform Architect
**Checked by:** architect-agent, development-agent, test-agent, build-agent, pipeline-agent

These constraints encode the approved patterns, banned practices, and platform boundaries
for this project's technology stack. They ensure consistency, security, and maintainability
across all features delivered by the multi-agent system.

> 📝 **To customise:** Replace the placeholder examples below with constraints specific to your stack.
> The universal constraints in Sections 1–4 apply to most projects; review before removing any.
> Add domain-specific tech constraints (e.g. Power Platform solution limits, AWS service restrictions)
> in Section 5.

---

## How to Read This File

| Column | Meaning |
|---|---|
| ID | Stable identifier — never changes |
| Constraint | What the rule requires or prohibits |
| Severity | `HARD` = gate blocker if violated · `SOFT` = warning, human decides |
| Scope | Which agents must actively check this |
| Rationale | Why this constraint exists |
| Verify By | How compliance is confirmed |

---

## Section 1: Security Constraints

| ID | Constraint | Severity | Scope | Rationale | Verify By |
|---|---|---|---|---|---|
| C-TECH-001 | No secrets, credentials, API keys, or tokens may be hardcoded in source code or config files committed to version control | HARD | development-agent, build-agent, test-agent | Credential exposure in git history | Secret scan in CI (e.g. `gitleaks`, `truffleHog`); code review checklist |
| C-TECH-002 | All secrets must be sourced from the approved secrets manager (Key Vault / CI secrets — see `knowledge/technology/build-and-deploy.md` and `coding-standards.md`) | HARD | development-agent, architect-agent | Centralised rotation and audit | Code review; TAD §6 documents secrets manager usage |
| C-TECH-003 | All API endpoints and service-to-service calls must use TLS 1.2 or higher | HARD | architect-agent, development-agent | Data in transit encryption | TAD §6 confirms; security test validates certificate and protocol version |
| C-TECH-004 | All user inputs must be validated and sanitised before processing or persistence | HARD | development-agent, test-agent | Injection attack prevention | Code review; security test with malformed inputs |
| ~~C-TECH-005~~ | *(status: retired 2026-08-19 — inapplicable to this stack.* `retired_reason`: *there is no SQL layer and no ORM anywhere in this solution. Data access is the Dataverse Web API and the Dataverse connector, both of which parameterise by construction; there is no string-concatenation surface for this rule to govern. It was scaffolding boilerplate that no agent could ever evaluate, and under the new `UNEVALUABLE` rule in `skills/how-to-apply-constraints.md` it would BLOCK every development-agent and test-agent gate rather than silently pass. Injection into the one place user text does reach an expression — the intake flow — is covered by `C-TECH-004`, which stays.)* | — | — | — | — |
| C-TECH-006 | Authentication must be enforced on all non-public routes and operations | HARD | architect-agent, development-agent, test-agent | Unauthorised access prevention | Security test: unauthenticated request → 401/403 |
| C-TECH-007 | Sensitive data (Tier 3+) must be masked or synthetic in all non-production environments | HARD | development-agent, pipeline-agent | PII exposure in lower environments | Pipeline config confirms masking step before deploy to Test/Acc |

---

## Section 2: Code Quality Constraints

| ID | Constraint | Severity | Scope | Rationale | Verify By |
|---|---|---|---|---|---|
| C-TECH-010 | All code must pass the project linter with zero errors before a build is packaged | HARD | build-agent | Consistent quality gate | `lint` step in `build.yml` exits non-zero on lint errors |
| ~~C-TECH-011~~ | *(status: retired 2026-08-19 — no verification exists.* `retired_reason`: *`Verify By` said "Code review; grep in CI" and no such grep has ever existed in `config/<slug>-build.yml`. A SOFT rule with no check is a comment, which `constraints/README.md` rule 5 and `agents/improvement-agent.md` anti-bloat limit 4 both forbid. If this matters, it is one `grep -rn 'TODO\|FIXME\|HACK'` build step away from being real — add the step and reinstate the row with a new id.)* | — | — | — | — |
| ~~C-TECH-012~~ | *(status: retired 2026-08-19 — no verification exists and no threshold was ever defined.* `retired_reason`: *`Verify By` was "Static analysis tool threshold"; no static analysis tool is installed, and `knowledge/technology/coding-standards.md` defines no complexity limit for it to read. Two undefined halves make an unevaluable rule.)* | — | — | — | — |
| ~~C-TECH-013~~ | *(status: retired 2026-08-19 — no verification exists.* `retired_reason`: *`Verify By` named `knip` / `pylint unused`; neither is installed and neither suits a repository that is mostly declarative solution XML and PowerShell. `pac solution check` (the `lint` build step) already reports unused and unreachable solution components, which is the part of this rule that applies here.)* | — | — | — | — |
| C-TECH-014 | Unit test coverage must meet the threshold defined in `knowledge/technology/coding-standards.md` | HARD | test-agent, build-agent | Regression prevention | Coverage report in CI; build fails below threshold |

---

## Section 3: Dependency Constraints

| ID | Constraint | Severity | Scope | Rationale | Verify By |
|---|---|---|---|---|---|
| ~~C-TECH-020~~ | *(status: retired 2026-08-19 — no package manifest exists to audit.* `retired_reason`: *this repository has no `package.json`, no `requirements.txt` and no `.csproj` with package references. The one third-party dependency that is actually pinned — Pester 5.7.1 — is pinned in the `unit-tests` build step and asserted by `src/tests/build/VerifyBuildConfig.Tests.ps1`, so that pin is enforced by a test rather than by this row. Reinstate with a new id when the Phase 3 Code App introduces a real manifest — at which point `npm audit` and a lockfile check make it verifiable.)* | — | — | — | — |
| ~~C-TECH-021~~ | *(status: retired 2026-08-19 — the scan it requires does not exist.* `retired_reason`: *`Verify By` was "Dependency scan step in `build.yml`; pipeline halts on HIGH/CRITICAL finding". There is no dependency-scan step, and with no dependency manifest there is nothing for one to scan. This is the exact shape of `IMP-0007` — a HARD gate that has recorded PASS since the day it was written while checking nothing. Reinstate together with `C-TECH-020` when the Code App lands.)* | — | — | — | — |
| ~~C-TECH-022~~ | *(status: retired 2026-08-19 — no dependencies, no licence scan.* `retired_reason`: *`licence-checker` / `pip-licenses` are named by `Verify By` and neither is installed; with no third-party packages there are no licences to check. Reinstate with `C-TECH-020` / `C-TECH-021`.)* | — | — | — | — |
| ~~C-TECH-023~~ | *(status: retired 2026-08-21 — nothing to audit, and no verification exists.* `retired_reason`: *the **fourth member of the family whose other three were retired on 2026-08-19** — `C-TECH-020` (pins), `C-TECH-021` (CVE scan), `C-TECH-022` (licences) — and simply missed in that sweep. It says the same thing about the same absent manifest: this repository has no `package.json`, no `requirements.txt` and no project package reference, so there are no dependency sources to approve. Its `Verify By` was "Architecture review; code review", which `constraints/README.md` rule 5 and improvement-agent anti-bloat limit 4 both forbid. Reinstate with a new id alongside `C-TECH-020`–`022` when the Phase 3 Code App introduces a real dependency manifest.)* | — | — | — | — |

---

## Section 4: Deployment & Environment Constraints

| ID | Constraint | Severity | Scope | Rationale | Verify By |
|---|---|---|---|---|---|
| C-TECH-030 | All deployments to Test, Acc, and Prd must use the managed/immutable artifact produced by the build-agent — no ad-hoc deploys | HARD | pipeline-agent | Consistency; traceability | Pipeline log references artifact manifest; no manual deploy steps |
| ~~C-TECH-031~~ | *(status: retired 2026-08-21 — no verification exists; superseded by `C-TECH-047` + `C-TECH-065`.* `retired_reason`: *`Verify By` was "Code review; `build.yml` `required_env_vars` block documents injection points", which is not a mechanically executable check. `C-TECH-047` enforces the hardcoded-value half with a real grep gate, `verify-build-config.py` and check 8 of `verify-pipeline-config.py` enforce the `required_env_vars` half, and `C-TECH-065` now covers the unresolved-placeholder case this row never checked. See the Retired Constraints table for the coverage argument.)* | — | — | — | — |
| C-TECH-032 | Every Prd deployment must have a corresponding Deployment Summary document committed to the repo | HARD | pipeline-agent | Audit trail | `docs/deployments/<slug>-deployment-summary.md` exists and is committed before pipeline closes |
| C-TECH-033 | Rollback must be possible for every Prd deployment; the rollback artifact must be verified before deploying forward | SOFT | pipeline-agent | Operational resilience | `pipeline.yml` `rollback_artifact` field is populated and tested |

---

## Section 5: Power Platform & Microsoft 365

| ID | Constraint | Severity | Scope | Rationale | Verify By |
|---|---|---|---|---|---|
| C-TECH-040 | In Test/Acc/Prd, Dataverse security roles are assigned only via Entra-group-backed **group teams** — never directly to individual users (see `knowledge/technology/security-model.md`) | HARD | architect-agent, development-agent, pipeline-agent, test-agent | Access is auditable and centrally governed via Entra group membership | TAD §6.1 mapping table; `post_deploy` script output; test-agent queries `systemuserroles_association` for direct assignments |
| C-TECH-041 | Tenant-level operations (create/modify app registrations, grant admin consent, create security groups, create SPO site collections, publish to the Teams org catalog) execute only behind the `APPROVE TENANT` gate and are recorded in the Deployment Summary | HARD | development-agent, pipeline-agent | Privileged, hard-to-reverse changes need explicit human authorisation and an audit trail | `tenant_prerequisites` block in `pipeline.yml`; Deployment Summary §Tenant-Level Operations |
| C-TECH-042 | All provisioning and `post_deploy` scripts are idempotent — check-before-create, safe to re-run, report `CREATED` / `EXISTS` / `FAILED` per resource | HARD | development-agent, pipeline-agent, test-agent | Pipeline retries and multi-environment runs must not create duplicates or fail spuriously | Script review at code-review gate; pipeline re-run produces `EXISTS`, not errors |
| C-TECH-043 | App registrations request least-privilege API permissions; broad permissions (`*.ReadWrite.All`, `Directory.*`, `Sites.FullControl.All`) require a justification in TAD §6 and an ADR | HARD | architect-agent, development-agent | Over-permissioned service principals are a tenant-wide attack surface | TAD §6 + ADR; permission list in `provisioning/entra/` script reviewed at gate |
| C-TECH-044 | Prefer federated credentials (OIDC) or certificates over client secrets for CI/CD and app-only auth; any client secret lives in Key Vault / CI secrets with rotation ≤ 180 days | SOFT | development-agent, pipeline-agent | Secrets leak and expire silently; federated credentials remove the secret entirely | Pipeline config review; Entra credential expiry report |
| C-TECH-045 | All connectors used by flows and apps must comply with the target environment's DLP policies — no mixing of business and non-business connector groups | HARD | architect-agent, development-agent, test-agent | DLP violations block solution import or silently disable flows in higher environments | TAD §4 lists all connectors; DLP policy check before build; import validation in Test |
| C-TECH-046 | Out-of-box security roles are never modified — copy into a `[PREFIX]` role and adjust the copy | HARD | development-agent, test-agent | OOB role changes are overwritten by platform updates and are untraceable | Solution diff shows no changes under OOB role IDs |
| C-TECH-047 | Environment-specific platform values (SPO site URLs, Teams team/channel IDs, Entra group object IDs) are supplied via Dataverse environment variables or deployment settings — never hardcoded in flows, apps, or scripts | HARD | development-agent, pipeline-agent | Hardcoded IDs break environment portability and cause cross-environment data leaks | Code review; deployment settings files contain all per-env values |
| C-TECH-048 | Code Apps access data only through managed connector data sources (`pac code add-data-source`) — no hand-rolled token acquisition or credential handling outside the approved custom-API module | HARD | development-agent, test-agent | Hand-rolled auth bypasses platform governance, DLP, and Dataverse security | Code review: no MSAL/token code outside the module documented in TAD §6 |
| ~~C-TECH-049~~ | *(status: retired 2026-08-18 — superseded by `C-TECH-060`.* `retired_reason`: *this was the **instance** where the class was `platform-field-length-limit-unenforced`. It hardcoded 256 and covered flow descriptions only; a second instance of the same class (`rev_setting.rev_description` at 500, `IMP-0009`) got its own separate script rather than a generalisation, which is the pattern `skills/how-to-promote-a-finding.md` §2 forbids. Coverage is not lost: the replacement gate fails on this constraint's own known-bad fixture — proven in `src/tests/build/BuildGates.Tests.ps1`.)* | — | — | — | — |
| C-TECH-050 | Entities/Attributes, Global OptionSets, Security Roles and Field Security Profiles are created via the Dataverse Web API on first creation in any environment — never assumed creatable by a first solution import | HARD | architect-agent, development-agent, pipeline-agent | Microsoft documents these as unsupported to create from scratch via solution import; solution import can only manage/update them once they exist | `provisioning/*/ensure-schema.ps1`-style idempotent script runs before the first solution import into a new environment; pipeline step order in `pipeline.yml` |
| C-TECH-051 | Hand-authored solution source never fabricates an id for a component Dataverse assigns on creation (Role, Field Security Profile, app-specific sitemap, model-driven app) — the live id is read back from the environment, or the component is referenced by `schemaName` where the component type supports it | HARD | development-agent, test-agent | A fabricated id either fails import outright (strict types, e.g. Role) or is silently ignored (lenient types, e.g. Field Security Profile) — both are worse than reading the real value once | Live id confirmed via Web API query before being committed to source; `verify-solution-root-components.py` cross-checks the declared id/schemaName against the on-disk definition |

---

## Section 6: Platform Contracts & Verification by Execution

Added after the first live deployment of a hand-authored solution, which cost fifteen import
attempts. Every failure was a plausible guess about a platform contract, committed to source, and
validated only by gates that could not detect it being wrong. Procedure:
`skills/how-to-verify-a-platform-contract.md`. Incident record:
`docs/development/revitalise-grant-automation-dev-deployment-handover.md`.

| ID | Constraint | Severity | Scope | Rationale | Verify By |
|---|---|---|---|---|---|
| C-TECH-052 | Every hand-authored platform contract (serialisation shape, file layout, field limit, id assignment, capability) not confirmed against ground truth — an artefact the platform itself produced — is recorded as a row in the **Unvalidated Assumptions Register**, Dev Summary §10, and carries an `A-nnn` comment at the point of the guess in source | HARD | architect-agent, development-agent, test-agent | Guesses are unavoidable when authoring ahead of a live environment; guesses that are not *tracked* are discovered one deploy failure at a time. On the feature that produced this constraint, every assumption the register flagged as unvalidated turned out to be wrong | Dev Summary §10 exists and has a row per guessed contract; test-agent cross-checks the register against the hand-authored artefacts and reports orphan guesses as a defect. **Mechanical half added 2026-08-19:** `python3 scripts/verify-component-shape.py src/solutions/<Name> --shapes constraints/technology/component-shapes.yml` (build step `component-shape`) exits 0 — once a contract HAS been ground-truthed, its proven element shape is recorded in that file and every future hand-authored component of that type is checked against it, so a closed assumption stays closed. Two findings closed this way rather than by two instance gates: `IMP-0045` (a comment before the root of an environmentvariabledefinition; four failed imports, `0x80040216` naming nothing) and `IMP-0037` (an option set missing the optionset-level `<Descriptions>`/`<displaynames>` that sit after `</options>`). Negative tests in `src/tests/build/BuildGates.Tests.ps1` |
| C-TECH-053 | A component is reported only at the verification level actually executed — well-formed (V1), packaged (V2), accepted by the target (V3), openable **and saveable by a human** in the designer/editor (V4), executed end-to-end (V5). V3 is re-run once to prove idempotency, and V4 is a named step with a named owner before any environment is declared deployed | HARD | development-agent, build-agent, test-agent, pipeline-agent | A successful pack proves layout, not content; a successful import proves the component was accepted, not that it works. Three of fifteen failures imported cleanly, were queryable via the API, and still could not be opened by a maker | Build manifest and Test Report §7 state the level reached per component; `pipeline.yml` declares the V4 step; Deployment Summary records its result |
| C-TECH-054 | Any script executed by CI or the pipeline runs on the CI runner's OS — OS-specific APIs, drives, and path assumptions (e.g. the Windows-only `Cert:` PSDrive, `\` separators, `Get-CimInstance`) are replaced with cross-platform equivalents or guarded and exercised on that OS | HARD | development-agent, build-agent, test-agent | A provisioning helper that had only ever run on Windows used a Windows-only API and would have failed every run on the Linux CI runner; it was caught only because provisioning was finally executed for real, on a Mac | Test suite executes the scripts on the CI runner OS in CI; code review flags OS-specific APIs |
| C-TECH-055 | Every warning emitted by a build, pack, or deploy tool is triaged — resolved, or recorded in the Dev Summary with an explicit rationale for accepting it. No warning is carried silently across builds | HARD | development-agent, build-agent, pipeline-agent | A pack warning that root components were "not defined in customizations" was present from the first build, ignored as noise, and was a precise report of the defect that later failed the import | Build log warning count reconciles to resolved-or-recorded entries in Dev Summary §7; build-agent lists unrecorded warnings in its constraint check output |
| C-TECH-056 | Diagnostic, temporary, or ground-truth components created in an environment during investigation are removed from the solution before export or promotion, and their creation and removal are recorded | HARD | development-agent, test-agent, pipeline-agent | Building a component for real is the correct way to obtain ground truth (`skills/how-to-verify-a-platform-contract.md` §3) — but anything left in the solution afterwards ships to every downstream environment | Solution component list diffed against the TAD §2 component inventory before export; Dev Summary §11 records each diagnostic component created and its removal |

---

## Section 7: Gate Integrity & the Learning Loop

Added 2026-08-17 after a week in which three of this project's own build gates were found to
have recorded PASS while checking nothing at all, and in which a working procedure established
one day was lost the next. Findings: `logs/improvement-log.jsonl`. Analysis:
`docs/improvements/2026-08-17-failure-analysis-and-self-learning-design.md`.

**Three constraints, one per recurring class** — not one per incident. That is the altitude
rule in `skills/how-to-promote-a-finding.md` §2 applied to its own founding data.

| ID | Constraint | Severity | Scope | Rationale | Verify By |
|---|---|---|---|---|---|
| C-TECH-057 | Every gate in a build or pipeline config is **proven able to fail**: it has a known-bad fixture under `src/tests/fixtures/known-bad/<gate>/` and a negative test asserting a non-zero exit, plus a positive test against real source. Config preflight passes before any build step executes. Exemptions are named in `scripts/verify-build-config.py` with a stated reason, never silent | HARD | build-agent, development-agent, test-agent, pipeline-agent | A gate that cannot fail is worse than no gate: it manufactures the confidence that stops anyone looking. Four instances of class `gate-cannot-fail` in one week — `gitleaks` scanning history instead of the working tree and recording PASS over none of the delivered source; `lint` pointed at a source folder where a packed `.zip` is required, ordered before the step that produces it, masked for four builds by being deferred; a HARD FR-016 compliance gate whose target path was missing its `.json`, so `grep`'s exit 2 was inverted into an unconditional pass from the day it was written; and a structural check satisfied by a marker inside an XML comment (`IMP-0002`, `IMP-0004`, `IMP-0007`, `IMP-0020`) | `python3 scripts/verify-build-config.py config/<slug>-build.yml` exits 0 — it fails the build if any gate lacks a registered negative test, if any step's inputs do not exist or are produced later, or if a tool receives the wrong input shape; `Invoke-Pester src/tests/build` green |
| C-TECH-058 | A Dev Summary §10 assumption still marked `OPEN` blocks deployment into any environment in which it could be closed. Proceeding requires the reviewer's explicit `OVERRIDE <A-nnn>` with a reason, recorded in the Deployment Summary | HARD | pipeline-agent, test-agent | `C-TECH-052` requires *recording* a guess; nothing required *closing* one. A-001 was recorded exactly as the process asks — guessed multi-select control classid, severity E2, `OPEN`, "pending V4" — and shipped anyway, reaching the reviewer as three fields rendering dropdowns with no options. The register predicted the defect precisely and was wired to nothing (`IMP-0014`) | pipeline-agent's assumption-register gate output lists every OPEN row and its closeability before Stage 0.5; Deployment Summary records each override with its reason |
| C-TECH-059 | The learning substrate is never destroyed: each build writes to its own `build/artifacts/<slug>-<date>-<n>/` (resolved by `scripts/resolve-artifact-dir.py`, never a literal path), findings are appended to `logs/improvement-log.jsonl` as they occur, and `logs/known-failure-modes.md` is regenerated so it is current with the log | HARD | build-agent, pipeline-agent, development-agent, test-agent, lead-agent | Three instances of class `learning-substrate-destroyed`. Six builds shared one hardcoded artifact directory, so the manifests for builds #1-#3 no longer exist and #6 overwrote #5 — and the manifest is the richest structured failure record this system produces. Eight findings were improvised into `logs/routing.log`, which nothing reads. A certificate-from-keychain procedure established on 08-16 was gone on 08-17 and the reviewer had to re-teach it (`IMP-0016`, `IMP-0022`, `IMP-0023`) | `python3 scripts/generate-known-failure-modes.py --check` exits 0; build manifest `artifact_path` is unique per build; the gate output's `IMPROVEMENT LOG:` line is present even when the answer is `none` |
| C-TECH-060 | **No shipped text value exceeds the length limit that governs it**, whether that limit is declared by a schema in this repo (`<MaxLength>` in `Entities/*/Entity.xml`) or fixed by the platform and declared by nothing (the Power Automate 256-character `description` cap). Limits are **read from the schema at check time, never transcribed into a script**, and every platform-fixed limit lives in one named table with its citation | HARD | development-agent, build-agent, pipeline-agent, test-agent | Two instances of class `platform-field-length-limit-unenforced`, each of which got its own instance gate instead of a generalisation: 62 flow `description` fields over the designer's save cap on the first DEV import (`C-TECH-049`, now retired), and 4 of 11 `rev_setting` seed rows rejected live by Dataverse mid-run after 7 had already written (`IMP-0009`). Neither gate would have caught the third instance, and neither followed a `<MaxLength>` change in the schema they were asserting against. `settingRows[].key` (`rev_name`, 100) and `.value` (`rev_value`, 4000) were covered by nothing at all | `python3 scripts/verify-field-length-limits.py src/solutions/RevitaliseGrantAutomation provisioning/deploymentSettings` exits 0 — it fails on an over-limit value, on an unreadable schema, and on a path with no scannable surface, so it cannot report PASS over nothing; `Invoke-Pester src/tests/build` green, including the two retired gates' own fixtures |
| C-TECH-061 | **The learning loop's own processing triggers are enforced by a script, not by memory.** `logs/improvement-log.jsonl` conforms to the schema in `skills/how-to-log-an-improvement.md` §2, and no `blocker`-severity entry sits at `status: NEW` without an explicit `deferred_reason`, and fewer than ten entries sit at `NEW` in total | HARD | lead-agent, improvement-agent, build-agent, pipeline-agent | `agents/WORKFLOW.md` has said since 2026-08-17 that a blocker routes to improvement-agent *immediately* and that ten `NEW` entries route at the next routing decision. Both rules existed only as prose in an agent file, and both have now failed **twice**: `IMP-0033` recorded 32 entries all still `NEW`, 23 of them describing changes that had already shipped, with four one-line knowledge proposals stranded behind them for four days; and on 2026-08-19 the queue stood at 23 `NEW` with **seven** unprocessed blockers while every other gate in CI was green. This project's own evidence is that a rule becomes effective when a script runs it — `C-TECH-049` worked once `verify-field-length-limits.py` existed, not when the row was written. A recorded `deferred_reason` is a reviewer's decision and does not trip the gate; an unread queue is not a decision | `python3 scripts/verify-improvement-log.py --check` exits 0 — it fails on a malformed entry, a duplicate id, an unprocessed blocker, a queue at the batch trigger, and on a missing or empty log, so it cannot report OK over nothing; wired into `.github/workflows/ci.yml` → `validate`; negative tests in `src/tests/build/BuildGates.Tests.ps1` |
| C-TECH-062 | **The pipeline config is preflighted before any stage runs**, to the same standard `C-TECH-057` sets for the build config: every step names an existing script, every `-Parameter` exists in that script's own `param()` block, every command shell-parses, no `{{PLACEHOLDER}}` survives into an executable step, the artifact path is resolved per run rather than written literally, and production declares a rollback route | HARD | pipeline-agent, development-agent, build-agent | `C-TECH-057` made the build config's gates provable and left the deploy config unchecked, which `IMP-0042` recorded as a `blocker` in as many words: *"A pipeline step can name a script that does not exist, a parameter that does not exist, or a path that does not exist, and nothing will say so until the stage runs against a live environment."* Two of the seven blockers open on 2026-08-19 were that one defect written twice — `ensure-schema.ps1 -AlternateKeysOnly` and `ensure-site.ps1 -LibraryOnly`, neither parameter existing — each found by a human, mid-deploy, in a real environment, and each decidable statically in milliseconds. A third instance was latent in the same file: `artifact:` still named a directory frozen on 2026-08-10, so `stage-dev`'s artifact check could only ever fail (`IMP-0016`'s class, in the file that constraint did not cover) | `python3 scripts/verify-pipeline-config.py config/<slug>-pipeline.yml` exits 0; wired into `.github/workflows/ci.yml` → `validate` **and, since 2026-08-21, as the `pipeline-config-preflight` step of `config/<slug>-build.yml`** — CI alone was not enough, because `IMP-0165` records that CI had never fired on a matching branch trigger, so this HARD gate had no live enforcement and a build could go green while the pipeline config it hands off to was red (`IMP-0175`, `blocker`); negative tests in `src/tests/build/BuildGates.Tests.ps1` cover the nonexistent-parameter, missing-script, literal-artifact-path, no-rollback and missing-config cases, and register the build step by name |
| C-TECH-063 | **Every file under `.github/` is validated before it is pushed**, locally and in CI: the YAML parses, every named-value in every expression is one GitHub allows *at that key*, every `needs:` names a real job, every local `uses: ./` resolves, and every `environment:` matches a key the pipeline configs declare. No `secrets` in any `if:` — ever | HARD | pipeline-agent, development-agent, lead-agent | `IMP-0074`: a certificate guard written as `if: ${{ secrets.PROVISION_CERT_PFX_BASE64 != '' }}` made `ci.yml` invalid, and because workflow parsing is all-or-nothing GitHub ran **zero jobs on every push for a day**. This is the only defect class CI structurally cannot report — no job runs, so no gate speaks, no check fails and the commit carries no red X; the sole signal was a notification email to the reviewer. It compounded the same day: `verify-improvement-log.py` detects duplicate finding ids exactly, a concurrent session had just produced one, and it never fired because it lives only in the workflow that was dead (`IMP-0080`). A repository whose gates all live in one workflow has a single point of silence, which is why this constraint binds the **local run** and not merely the CI copy | `python3 scripts/verify-workflow-syntax.py` exits 0, run locally before any commit touching `.github/` and wired as the FIRST step of `.github/workflows/ci.yml` → `validate`; `python3 scripts/verify-workflow-syntax.py --selftest` exits 0, proving the gate still rejects five known-bad shapes and still accepts a valid one; negative tests in `src/tests/build/BuildGates.Tests.ps1`. Coverage proof: the gate reports all four `secrets`-in-`if:` errors against `88dd590`'s `ci.yml`, at the same four lines GitHub named |
| C-TECH-064 | **Environment state that solution source cannot express is verified LIVE against the declared intent, from a list derived from source, after every deploy into that environment.** Organisation and table audit switches, audit retention, option-set membership, field-permission membership and role bindings are all in this category: an import can neither set nor remove them, so source is silent and a source-side check proves nothing about the environment | HARD | pipeline-agent, test-agent | `IMP-0082` (blocker): DEV held special-category health data with NO audit trail from 2026-08-14 to 2026-08-19 — organisation `isauditenabled=False` and `IsAuditEnabled=False` on all five tables — while four Test Report revisions recorded `C-DOM-010` PASS from the ATTRIBUTE-level flags in solution source, which do nothing while the two switches above them are off. Entity-level `IsAuditEnabled` is absent from every `Entity.xml`, so no import could have set it. `IMP-0019` is the same property from the other direction: import RELABELS matching option values and never deletes omitted ones, so orphans survive every later import and only a live comparison finds them. `IMP-0084` is why the check must be a query and not a portal confirmation | The environment's `verification:` block declares a step per category that READS live state and compares it to source: `organizations?$select=isauditenabled,auditretentionperiodv2`, `EntityDefinitions(LogicalName='x')?$select=IsAuditEnabled` for every entity folder under `Entities/`, `GlobalOptionSetDefinitions` against `OptionSets/*.xml`, `fieldpermissions` against every `IsSecured=1` column, **column-security profile MEMBERSHIP** (`teamprofiles` / the profile's associated teams, against `dataverse.columnSecurityProfiles[].memberTeams` in the environment's settings file — membership is who the profile grants access TO, and it lives in no solution file, so source is silent about the one thing that decides disclosure), and `systemuserroles` for direct assignments. The step fails on any difference. A source-only assertion may never be recorded as evidence for this row. **For a Dataverse-triggered cloud flow, two further kinds of evidence are inadmissible** (added 2026-08-21): (a) a metadata assertion — `statecode`, the existence or `createdon` of a `callbackregistration`, `subscriptionRequest/scope`, `runas` — may NOT be recorded as evidence that the trigger works. Six successive findings each added one more field to this checklist and each was defeated by the next incident; the terminal case passed every one of them and did not fire for 12 rows in 9 minutes. The evidence is an OBSERVED EFFECT from a row created after the last change to the flow: `pwsh provisioning/dataverse/verify-flow-trigger.ps1 -Env <env>` exits 0. (b) A run reached by **Resubmit** from run history is not such evidence — it replays the original trigger payload and never touches the subscription path, so it proves the action logic and nothing about the trigger. A batch of rows scored long after creation, in reverse creation order, is the signature of a replay, not of delivery |
| C-TECH-065 | **A credential that authenticates is not an identity that is authorised.** Before any script is trusted against a target environment, a minimal identity probe against **that exact environment** must succeed, and the pipeline config must declare it before the first step that depends on it. Acquiring a token proves the directory accepted the audience; it never proves the target system has provisioned the caller. The same rule governs any settings value that step needs: a settings file that RESOLVES is not a settings file whose values are RESOLVED | HARD | pipeline-agent, development-agent, build-agent, test-agent | Two instances in 24 hours, in two different target systems, both blockers. `IMP-0105`: `Connect-ProvisioningGraph` succeeded and `Get-MgApplication` returned `Authorization_RequestDenied` — *"A successful Graph connection proves the credential, never the permission."* `IMP-0146`: token acquisition against the TST/ACC org succeeded and every Dataverse call including `WhoAmI` returned `0x80072560 — the user is not a member of the organization`, while the identical code, tenant, app id and certificate resolved a `UserId` against DEV. A Dataverse application user is created per environment. `config/<slug>-pipeline.yml` gave `dev` an `environment_prerequisites` block for exactly this and gave `tst_acc` and `prd` none, so eleven and ten executable provisioning steps respectively assumed access nobody had established. `IMP-0145` / `IMP-0147` are the settings half of the same property: check 10 of the pipeline preflight resolved the settings FILE and never opened it, so the gate printed `runtime settings files resolved: 31` and `PASS` over a `tst_acc` block whose first post-deploy step was guaranteed to throw | `pwsh provisioning/dataverse/verify-environment-access.ps1 -Env <env>` exits 0 and prints `PASS — provisioning identity recognised by <org url>` (verified live against DEV and TST/ACC on 2026-08-21), **and** `python3 scripts/verify-pipeline-config.py config/<slug>-pipeline.yml` passes checks 11 and 12 — every environment running provisioning declares the probe before its first provisioning step, and every settings file those steps resolve is OPENED, with each unresolved `{{TOKEN}}` either fixed or declared in that file's `_unresolved` block with an owner, a reason and an unexpired date. Negative tests: `src/tests/build/BuildGates.Tests.ps1` |
| C-TECH-066 | **The approved TAD's schema and access tables are a CHECKED SPECIFICATION, not prose.** Every column the TAD names must exist in the matching `Entity.xml` or be declared in an owned, dated deferral; and every column the TAD marks visible to a persona must sit on a table that persona's role holds a `prvRead` for. The two tables are written by different passes and may not silently disagree | HARD | architect-agent, development-agent, test-agent, build-agent | **An absent column is absent from both sides of every schema gate this repository has.** Each of them compares solution source against other solution source — `IsSecured` against the field security profile, `RootComponents` against disk, form cells against attributes — so a column the TAD requires and nobody built is invisible to all of them. `IMP-0158`/`IMP-0159`: twelve columns the approved TAD names were missing for eleven days while every schema gate passed. The access half is the same gap with teeth: TAD §3.1 marked two applicant columns trustee-visible while §6.2 gave the trustee role no read privilege on that table, and **column security *releases* a column, it never *grants* table access** — the two statements are mutually unsatisfiable, which is why FR-034 was unimplementable as written and nothing anywhere compared them. This is the first executable check over the TAD; a document that governs the schema while being reviewed only as prose was the largest unguarded surface left in the system | `python3 scripts/verify-tad-coverage.py` exits 0, wired as the **`tad-coverage` step** in `config/<slug>-build.yml` — *grep that `steps:` block to check this row, never an improvement review's APPLIED cell: on 2026-08-20 this Verify By said "wired as a build gate" while no config anywhere named the script, and the review that claimed it stood unchallenged for a day (`IMP-0174`, `blocker`)*. It **fails when it parses zero rows** rather than passing over nothing (`IMP-0007`'s shape — it reads a markdown table, so a reword must break it loudly, never silently), and it fails on a deferral carrying no owner or no date. It cannot judge whether the TAD is *right*, only whether source agrees with it. Negative tests: `src/tests/build/BuildGates.Tests.ps1` |

---

## Retired Constraints

| ID | Constraint (summary) | Retired | Reason |
|---|---|---|---|
| C-TECH-031 | Environment-specific values are not embedded in the artifact — they are injected at deploy time | 2026-08-21 | **No verification exists, and the substance is enforced twice elsewhere.** `Verify By` read *"Code review; `build.yml` `required_env_vars` block documents injection points"* — the same shape as `C-TECH-011`/`012`/`013` above, which `constraints/README.md` rule 5 and improvement-agent anti-bloat limit 4 both forbid. **C-TECH-047** covers hardcoded environment values with a real gate that greps the whole solution tree case-insensitively for the organisation and SharePoint host patterns (`IMP-0119` records it firing on an example URL inside a comment), and the `required_env_vars` half is enforced mechanically by `verify-build-config.py` and by check 8 of `verify-pipeline-config.py`. **Coverage proof differs in kind here and the difference is the argument:** this row has no known-bad fixture to re-run, because it never had a gate to fixture. Nothing regressed on retirement, because nothing was executing. `C-TECH-065` now additionally covers the case this row's text gestured at and never checked — a deployment settings value that is still a `{{PLACEHOLDER}}` at deploy time |
| C-TECH-049 | No Power Automate flow `description` field may exceed 256 characters | 2026-08-18 | Superseded by **C-TECH-060**, one schema-driven gate for the whole class `platform-field-length-limit-unenforced`. Two instance gates existed for one class (`verify-workflow-description-length.py`, `verify-setting-description-length.py`), both hardcoding their limit, with nothing covering a third instance. Coverage proven not lost: `verify-field-length-limits.py` exits non-zero against **both** retired gates' known-bad fixtures, asserted in `src/tests/build/BuildGates.Tests.ps1`. Both scripts deleted; both fixtures kept as the coverage proof. `IMP-0009`, and the standing candidate recorded in the 2026-08-17 retirement check below |
| C-TECH-005 | Parameterised queries / ORM — no string concatenation | 2026-08-19 | **Inapplicable to this stack.** No SQL layer and no ORM exists; data access is the Dataverse Web API and connector, which parameterise by construction. Injection where user text *does* reach an expression stays covered by `C-TECH-004` |
| C-TECH-011 | No `TODO` / `FIXME` / `HACK` above Test | 2026-08-19 | **No verification exists.** `Verify By` named a "grep in CI" that has never been a step in any build config |
| C-TECH-012 | Cyclomatic complexity threshold | 2026-08-19 | **No verification and no threshold.** No static-analysis tool is installed and `coding-standards.md` defines no limit for one to read |
| C-TECH-013 | No dead code committed | 2026-08-19 | **No verification exists.** `knip` / `pylint` are named but neither is installed nor suits a declarative-XML + PowerShell repository. `pac solution check` (the `lint` step) covers the solution-component half |
| C-TECH-020 | Dependencies pinned to exact versions | 2026-08-19 | **Nothing to audit.** No `package.json`, `requirements.txt` or `.csproj` package reference exists. The one real pin (Pester 5.7.1) is asserted by `VerifyBuildConfig.Tests.ps1`. Reinstate with a new id when the Phase 3 Code App introduces a manifest |
| C-TECH-021 | No HIGH/CRITICAL CVE deployed to Acc or Prd | 2026-08-19 | **The scan it requires does not exist**, and with no manifest there is nothing to scan. HARD, and therefore recording PASS over nothing since it was written — `IMP-0007`'s shape exactly. Reinstate with `C-TECH-020` |
| C-TECH-022 | Open-source licence compatibility | 2026-08-19 | **No dependencies, no licence scan.** Reinstate with `C-TECH-020` / `C-TECH-021` |
| C-TECH-023 | New dependencies must come from sources approved in `stack-overview.md` | 2026-08-21 | **Fourth member of the `C-TECH-020`–`022` family, missed in that sweep.** Same absent manifest, same conclusion: no `package.json`, no `requirements.txt`, no project package reference, so there is nothing to approve a source *for*. `Verify By` was *"Architecture review; code review"* — the non-mechanical shape `constraints/README.md` rule 5 forbids for any row. Coverage proven not lost, **vacuously and deliberately**: there was never a gate, so there is no fixture to re-run and nothing regresses. Reinstate with a new id alongside `C-TECH-020`–`022` when the Phase 3 Code App introduces a real dependency manifest. Carried unapplied through reviews 4 and 5, applied 2026-08-21 |

**Retirement check, 2026-08-17:** 59 constraints reviewed while adding Sections 6–7. No
constraint is currently redundant — `C-TECH-049` (flow description ≤256) and the
`rev_setting.rev_description` ≤500 gate are two *instances* of one class and are the standing
candidate for consolidation into a single schema-driven field-length gate, but consolidating a
HARD compliance gate is a behavioural change that must go through
`APPROVE IMPROVEMENTS` rather than being folded into the change that created the mechanism.
Carried as the first item for improvement-agent's first review.

**Retirement check, 2026-08-18** (`docs/improvements/2026-08-18-improvement-review.md`): the
item carried above was **done** — `C-TECH-049` is retired and `C-TECH-060` replaces it, which
is this file's first retirement in 60 constraints. One further candidate was considered and
**rejected**: `C-TECH-052` (record every guessed platform contract) looks redundant beside
`C-TECH-058` (an `OPEN` assumption blocks the deploy), but they act at different moments —
052 makes the guess visible at authoring time, 058 stops it shipping. Retiring 052 would
leave 058 with nothing to read. No other constraint is currently redundant.


**Retirement check, 2026-08-19** (`docs/improvements/2026-08-19-improvement-review.md`): the
first retirement pass driven by *coverage* rather than by consolidation. Every active row's
`Verify By` was classified as mechanically executable or not: of 55 active constraints, **12**
named anything a machine could run and **8** corresponded to a check that actually exists. Seven
rows were retired above — not because their subjects do not matter, but because each named a
tool, manifest or technology this project does not have, so no agent could ever evaluate them.
Under the `UNEVALUABLE` rule added to `skills/how-to-apply-constraints.md` in the same review
they would from now on **BLOCK** every gate whose scope they are in, rather than silently pass;
retiring them is the honest resolution, and each names what would have to exist to reinstate it.

Coverage is not lost, because there was none: no fixture, script or test asserted any of the
seven. That is the finding, not a caveat. Three rows were added in exchange (`C-TECH-061`,
`C-TECH-062`, `C-DOM-032`), each with a script, a known-bad fixture and a negative test — so the
file is **eight rows shorter and four executable gates richer**.

---

## Constraint Violation Response

When a HARD technology constraint is violated, the agent must:

1. Stop the current task immediately (do not complete the document or package the build)
2. List the violated constraint IDs in the gate output under `CONSTRAINT CHECK`
3. Emit `BLOCKED` status
4. Do not proceed until the violation is resolved and the check re-runs clean

When a SOFT technology constraint is violated, the agent must:

1. Document the violation in the gate output under `CONSTRAINT CHECK` as a warning
2. Include a brief explanation and a proposed resolution or accepted risk
3. Proceed to gate — the human reviewer makes the final call

When a constraint cannot be evaluated at all — its rule text is a placeholder, or its
`Verify By` names a tool, manifest or technology this project does not have — the agent
records it `UNEVALUABLE`, **never `PASS`**. A HARD `UNEVALUABLE` row blocks the gate exactly
as a violation does, and is resolved by `improvement-agent` behind `APPROVE IMPROVEMENTS` —
by writing the rule, or by retiring the row. Procedure and output format:
`skills/how-to-apply-constraints.md` Step 3. This outcome did not exist until 2026-08-19, and
its absence is why seven rows retired above could report PASS while checking nothing
(`IMP-0035`).
