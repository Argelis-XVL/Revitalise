function openGetAlertPageDialog(primaryControl) {
    var pageInput = {
    pageType: "custom",
    name: "ad_getalert_76344", // Replace with your custom page logical name
    entityName: "ad_sanctionalert"
    };
    
    var navigationOptions = {
    target: 2, // Opens as dialog
    position: 1, // Center
    width: { value: 50, unit: "%" },
    title: "Add new Alert"
    };
    
    Xrm.Navigation.navigateTo(pageInput, navigationOptions).then(
    function () {
    primaryControl.data.refresh(); // Refresh after close
    }
    ).catch(function (error) {
    console.error(error);
    });
    }