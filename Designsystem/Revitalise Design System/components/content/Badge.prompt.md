Solid pink circular social icon, matching the header/footer social row. Substituted brand glyphs (no real icon SVGs were found in source — see readme Iconography section).

```jsx
<Badge network="instagram" size={40} />
```
