Rounded status/explanation panel — replaces the grey-box-with-dark-left-border pattern seen in the trustee portal with a flat tinted card (no left accent bar). Use `muted` for "not available yet" states, `info` for pink-tinted callouts, `warning` sparingly.

```jsx
<Notice tone="muted" title="Round figures are unavailable">The round-statistics flow is not bound to this app.</Notice>
```
