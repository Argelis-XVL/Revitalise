Simple labelled text field matching the newsletter-signup form on revitalise.org.uk.

```jsx
<Input label="First name" required placeholder="Jane" />
```

Square corners (`--radius-sm`), thin grey border, no fill. Use `type="email"` for email fields.
